1. Создать файл:
```bash
sudo nano /etc/modprobe.d/disable-nvidia-nouveau.conf
```

2. Добавить в него:
```bash
blacklist nouveau
blacklist nvidia
options nouveau modeset=0
```
3. Затем:
```bash
sudo update-initramfs -u
```
